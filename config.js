// Boatyard brand survey — connection settings
//
// email    : where responses get emailed. Already set — nothing to do here.
// endpoint : optional. Only for the Apps Script route, where responses land in
//            a Google Sheet automatically. Leave the placeholder to skip it.

window.BY_CONFIG = {
  email:    "camila.boatyard@gmail.com",
  endpoint: "PASTE_YOUR_EXEC_URL_HERE"
};
